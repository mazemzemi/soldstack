name: CI - Common

on:
  push:
    paths:
      - "backend/common/**"
  pull_request:
    paths:
      - "backend/common/**"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "24"

      - name: Build Common Module
        run: ./gradlew :backend:common:build
