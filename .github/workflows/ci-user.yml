name: CI - User Service

on:
  push:
    paths:
      - "backend/user-service/**"
      - "backend/common/**"
  pull_request:
    paths:
      - "backend/user-service/**"
      - "backend/common/**"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "24"

      - name: Build User Service
        run: ./gradlew :backend:user-service:build
